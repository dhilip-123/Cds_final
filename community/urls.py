# from django.urls import path, include
# from rest_framework.routers import DefaultRouter
# from .views import CommunityViewSet, UserViewSet,ElectionOfficerViewSet,CommunityAdminViewSet

# router = DefaultRouter()
# router.register(r'communities', CommunityViewSet)
# router.register(r'users', UserViewSet)
# router.register(r'community-admins', CommunityAdminViewSet)
# router.register(r'election-officers', ElectionOfficerViewSet)

# urlpatterns = [
#     path('', include(router.urls)),
# ]
